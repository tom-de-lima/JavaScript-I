let velocidade = 100

while (velocidade > 0) {

    alert("O carro está a " + velocidade + " km/h")
    velocidade -= 20
    alert("Reduzindo 20 km/h")

    //Caso deseje encerrar o looping quando o valor da variável "velocidade" atinja 40 km/h

    // if (velocidade === 40) {
    //     break
    //}

}

alert("O carro parou.")

